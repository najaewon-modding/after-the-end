name: Apply Basecamp Joint Planner

on:
  push:
    paths:
      - ".github/workflows/apply-basecamp-joint-planner.yml"

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: temurin
      - name: Apply planner patch
        run: python tools/patch_basecamp_joint_planner.py
      - name: Build
        run: |
          chmod +x gradlew
          ./gradlew build
      - name: Commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/main/java/net/njw/aftertheend/city/basecamp/BasecampPlacementService.java \
                  src/main/java/net/njw/aftertheend/city/basecamp/BasecampPlacementPlanner.java
          git rm tools/patch_basecamp_joint_planner.py
          git rm .github/workflows/apply-basecamp-joint-planner.yml
          git commit -m "refactor: jointly optimize basecamp chunk placement"
          git push
